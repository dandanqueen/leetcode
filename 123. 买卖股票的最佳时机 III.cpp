class Solution {
public:
    int maxProfit(vector<int>& prices) {
    	int size = prices.size();
        vector<int> dp1(size);
        vector<int> dp2(size);
        for (int i=0; i<size; i++){
        	
        }
    }
};